@model Brand

<form enctype="multipart/form-data" asp-action="Edit">
    <section class="h-100 bg-light">
        <div class="container py-5 h-100">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col">
                    <div class="card card-registration my-4">
                        <div class="row g-0">
                            <div class="col-xl-6">
                                <div class="card-body p-md-5 text-black">
                                    <h3 class="mb-5 text-uppercase">Update product</h3>
                                    <input type="hidden" asp-for="BrandId" value="@Model.BrandId" />
                                    <div class="form-outline mb-4">
                                        <input asp-for="BrandName" type="text" id="BrandName" class="form-control form-control-lg" value="@Model.BrandName" />
                                        <label class="form-label">Product Name</label>
                                    </div>
                                    <div class="d-flex justify-content-end pt-3">
                                        <button type="button" class="btn btn-light btn-lg">Reset all</button>
                                        <input type="submit" value="Update" class="btn btn-success btn-lg" />
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        </div>
    </section>
</form>
